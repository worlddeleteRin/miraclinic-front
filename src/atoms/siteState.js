import { atom, selector } from "recoil";


export const requestCallModalOpenState = atom({
    key: 'requestCallModalOpenState',
    default: false, 
});

export const mobileMenuDrawerOpenState = atom({
    key: 'mobileMenuDrawerState',
    default: false,
});

